"use strict"; // treat all JS code as newer version

// alert( 3 + 3 ) //we are using nodejs, not browser

//Data Types

/* 1. number
2. undefined -> types is undefined only
3. string -> collection of characters. Represented within double quotes.
4. null -> standalone value. It is of 'object' type.
5. boolean -> true/false
6. bigint -> used for very large numbers

To check the type of any value in JS, use 'typeof' keyword.
Ex- console.log(typeof "Abhay")
*/
